services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - meta_app_data:/app/data
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.results.entrypoints=websecure
      - traefik.http.routers.results.rule=Host(`results.ozmo.media`)
      - traefik.http.routers.results.tls.certresolver=lets
      - traefik.http.services.results.loadbalancer.server.port=3000
    networks:
      - default
      - web

volumes:
  meta_app_data:

networks:
  web:
    external: true
